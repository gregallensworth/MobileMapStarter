Splash screens must be PNG files.

This folder is often very large, and can push the 10 MB limit of Phonegap Build.
You can significantly reduce file size by using 256-color 8-bit PNGs (Indexed color mode),
since most splash screens do not need transparency (unlike icons).
